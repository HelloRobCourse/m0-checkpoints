#include <iostream>

int main() {
    float num1, num2;  // Variables to store the numbers.
    char op;  // Variable to store the operator.
    
    // Ask for the first number.
    std::cout << "Please type a number and press enter: ";
    std::cin >> num1;

    // Ask for the operator.
    std::cout << "Please type a math operator (one of: + - * or /): ";
    std::cin >> op;

    // Ask for the second number.
    std::cout << "Please type another number and press enter: ";
    std::cin >> num2;

    // Write your code here! 
    // Use the numbers and operator input by the user above to compute 
    // the operation. To pass the tests, make sure you print out the full
    // operation followed by the result (ex. 1 + 1 = 2).

}
